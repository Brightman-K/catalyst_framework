# 设计思路说明

##前言

本设计的出发点是通过机器学习，快速找到优秀催化剂的共有特征，并在此基础上拓展。

##问题分析及解决

从以下问题的基础上解决这一问题：
1.	DFT数据不同文献并不一定互通。VASP计算需要设计磁矩等参数，以及BMIX等收敛参数，并且数据和建模精度有密切关系，故而不同文献的DFT数据并不一定互通；而对于单个课题组而言，高通量计算需要大量的成本和时间，所以微观数据（或称之为特征）的采集具有很大的困难。
2.	快速筛选优秀的催化剂，进行实验验证是设计此思路的目的。
3.	过拟合的原因有很多，例如模型过于复杂导致样本较少的情况下样本的表现为随机的噪声也被拟合入内，再比如样本之间距离过大，没有通过扰动形成不同样本附近的样本集，导致模型无法贴合实际的未拟合区间。我的设计确实可能存在过拟合问题，但我的思路就是：先不考虑过拟合（设计思路的时候，我是指），先按照精确符合训练集的方式进行设计，寻找出符合训练集的ML模型。为什么这么做？我基于自己的两个观点：
A并不是主观上设计成完美符合训练集就一定代表过拟合。这基于以下假设：
1.假设全体催化剂（不止此样本集的所有世界上的样本）存在不止一个底层描述公式。也就是不同样本间，当描述的深度在一定程度时，样本不同类之间的描述公式并不相同。
2.假设催化剂某一性质除了给定的特征以外，还存在未指定的隐变量进行描述。
3.一些催化剂的隐变量之间具有一致性，使得即使不指定隐变量，给定变量也能描述出这个催化剂小集合的局域特征。
4.到底是不是过拟合，需要测试集检验，而测试集如果太小，就可能找不到符合拟合公式的测试集样本；而且考虑到是为了寻找局域的规律而非统一于所有样本的规律，故而并不需要全体测试集乃至训练集进行拟合。
B我的目的是在成功样本上寻找一个局域规律，即并不是针对于全体的规律，这就需要对训练集和测试集进行处理（此算法主体并非此工作，这一问题在其他工作中体现）。此工作主要针对训练隐变量模型。
4.	涉及的样本是成功样本。他们的目标性质很可能差距不大，而且差距大并不能完全说明优劣，因为很可能受到电解质浓度等影响，所以需要重新设计一套打分公式。

##思路概述

通过决策树算法，通过对打分公式以及树的迭代，使得最终叶子内的样本彼此分数之间的差距尽可能小。随后通过数学分析，分析未进行分类的特征的隐变量情况，并检验隐变量加入是否有利于重新区分。如果成功，则进行多元分类以及精确性能预测的神经网络的设计。

##思路详解

针对不同的催化剂-环境体系：
存在体系A,B,C,….X  
选取特征a，b，c，d
体系存在打分机制 使得A变为f（A） B变为f(B) 以此类推。通过打分来识别不同体系是否一致，打分公式中在迭代一定轮后也引入隐变量。打分公式初始形式涉及了各种特征以及拉齐项（例如，高电解液浓度天然产率高，所以需要惩罚项，使得不同电解液浓度之间可以比较）
打分公式可迭代每个项的权重-打分公式1-下述决策树同时跑，且每个数内部多次抽样取平均-打分公式2-下述树是否更好，迭代。
特征阈值不需要迭代，树自己会选最优。每轮树种类都一样。
通过决策树拟合。平行多颗树，不同树自由选择分类的特征以及特征的阈值。
每棵树得到一组有先后顺序的特征-阈值组
观察不同特征-阈值组对应的最终产出的叶子内部分数间方差的大小。
每棵树进行十次，每次抽样随机种子不一致，削减运气成分。
同时引入置换检验等，证明确实特征组合是好的。以及通过新的集合测定。
保留优秀的特征-阈值组合。以及最终的打分公式。
对优秀的特征-阈值组合中排除偏离均值的样本，使得打分趋于一致，说明隐变量存在一致性（这一段并非此工作的内容，这确实需要考虑循环论证，但是在材料领域能结合实验说明言之有理。此工作中不必过度关注）
通过独热编码，拟合每个样本为n行向量，加入非打分的特征，有/无占一行。是否处在有 的某个区间细分化 占大量行，进行统计分析—得到假设此特征独立于其他特征的数学分布；同时分析各特征 A出现 B出现/不出现的分布 以及A,B出现 C出现/不出现的分布，以及A B不出现 C出现/不出现的分布，得到假设条件出现的分布。
上述如果找到，则标记，用新的样本集进行老特征阈值+新特征的打分方式，观察是否成立。不断迭代找特征。当找到特征能够非常精确的区分后（此时可以理解为特征已经足够进行精确数值拟合了。（事实上，有效特征足够多也等价于可以精确数值拟合）这部分我坚持自己的观点 因为保底也是 总有一个局域满足我说的话）
如果不成立，也能说明：如同次要工作一般，局域精描述，可用于快速筛选。
如果成立，则说明找到了隐变量是谁。
进阶，如果能找到，则开始用神经网络对数值进行精确拟合。同时也加入自由可梯度下降的向量，分析其他决定的隐变量。
进阶平行，如果能找到，用神经网络对数值进行范围拟合，具体而言：
数值人为划定区域 属于某区域用同一个数代替，区域划定可以接入反向传播；同时也加入自由可梯度下降的向量，分析其他决定的隐变量。
至此完成了二元判别问题到多元判别问题的跃迁。
如有余力，训练势函数，使得势函数内的参数与上述找到的特征存在一致性，这样就找到了势函数的参数的训练方法，没必要跑VASP了。这是有可能的。

##问题的辩论

如果测试集表现差怎么办？回到局域规律问题，过拟合是个现象而不是必然结论，寻找更多样本观察是否存在能比较好符合的，能找到就说明这是个局域规律，那么上述思路就归类到了局域规律的寻找。

##创新性说明

打分公式和决策树互相影响，并非孤立设计，提高其联动性。
树种类跨轮锁定，人为锁定住树种类，而不是每次随机出树。
隐变量发现特征-打分评估隐变量-继续搜索隐变量迭代，发现多种隐变量特征。并结合消融实验说明。

##结尾-想说的话

本工作是个人在材料领域学习实践中的思考性工作。更多作为思路展示。
本设计大部分思路是自己想的，后续搜索文献发现才知道其中的很多部分也有前人做过。目前我没发现整套思路高度相似/完全一致的。
本设计思路全程架构由自己完成，写这个WORD是方便非ML科班出身的同志观看。
如果没有时间比我更早（我的时间标志是Github发布时间）的发布，则后续发现完全撞车或者高度相似的工作务必引用并说明这是我的成果。
如果大家发现思路设计有其他需要讨论的问题，或者存在高度相似/完全撞车的早于我的全流程工作欢迎大家留言，我看到后将及时补充说明。
思路方面没问题，代码方面的问题后续将进一步核查。如有代码错误欢迎指正。
